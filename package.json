{
  "name": "altudo-dashboard",
  "version": "3.1.0",
  "description": "Altudo executive dashboard with live Asana reporting and grounded ChatGPT intelligence",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "test": "node --test",
    "check": "node --check lib/asana.js && node --check lib/reporting.js && node --check lib/ai.js"
  },
  "dependencies": {},
  "devDependencies": { "vercel": "^34.0.0" },
  "engines": { "node": ">=18" }
}
