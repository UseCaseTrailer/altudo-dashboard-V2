{
  "name": "altudo-dashboard",
  "version": "1.0.0",
  "description": "altudo.co Executive Dashboard — live Asana data via Vercel serverless API",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "test": "node --test",
    "check": "node --check lib/asana.js && node --check lib/reporting.js"
  },
  "dependencies": {},
  "devDependencies": { "vercel": "^34.0.0" },
  "engines": { "node": ">=18" }
}
